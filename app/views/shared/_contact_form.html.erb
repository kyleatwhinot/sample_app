<%= form_for(@contact) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <%= f.text_field :name, placeholder: "contact name..." %>
  <%= f.submit "Add name", class: "btn btn-primary" %>
<% end %>